{
  "name": "github-branch-cleaner",
  "version": "1.0.0",
  "description": "A CLI tool to clean up local Git branches based on GitHub PR status",
  "main": "index.js",
  "bin": {
    "github-branch-cleaner": "./index.js"
  },
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["git", "github", "branch", "cleanup", "cli"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "commander": "^14.0.0",
    "dotenv": "^16.5.0"
  }
}
